{
  "name": "@empire/asset-loader",
  "version": "0.1.0",
  "private": true,
  "description": "Cliente de carregamento de assets usado pelos jogos: entrega AssetDescriptor ao GameEngine com cache em memoria, evitando requisicoes de rede duplicadas para o mesmo asset.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@empire/engine-core": "workspace:*",
    "@empire/engine-types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
