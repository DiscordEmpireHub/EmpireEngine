{
  "name": "@empire/engine-types",
  "version": "0.1.0",
  "private": true,
  "description": "Tipos compartilhados do EmpireEngine (assets, comandos de render). Sem lógica, sem dependência de Phaser.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
